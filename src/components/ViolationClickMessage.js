import React from 'react';
import {violationClickMessage as style} from '../style';

/**
 * Click On Violation Message
 * @returns {React.Component}
 */
export const ViolationClickMessage = () => (
    <span style={style}>Click to find out more about the Building's Violation!</span>
);

export default ViolationClickMessage;

 
